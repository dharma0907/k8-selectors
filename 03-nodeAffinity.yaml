apiVersion: v1
kind: Pod
metadata:
  name: with-node-affinity
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution: # this is used to force the pod, hard rule, if hard rule pass then check for soft rule, if hard rule fails then pod will not be scheduled
        nodeSelectorTerms:
        - matchExpressions:
          - key: topology.kubernetes.io/zone
            operator: In
            values:
            - us-east-1d
            - us-east-1f
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 50
        preference:
          matchExpressions:
          - key: topology.kubernetes.io/zone
            operator: In
            values:
            - us-east-1d
      - weight: 100 # weight is used to give preference to a node, higher the weight more the preference 1 t0 100 value on;y acceptable
        preference:
          matchExpressions:
          - key: topology.kubernetes.io/zone
            operator: In
            values:
            - us-east-1f
  containers:
  - name: nginx # container name
    image: nginx # by default it will get from docker hub